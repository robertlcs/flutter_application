import 'package:flutter/material.dart';

class MusicPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return new Scaffold(
      appBar: new AppBar(
          title: new Text('这是测试的首页')
      ),
      body: new Center(
        child: new Text('Hello WorDFADld'),
      ),
    );
  }
}